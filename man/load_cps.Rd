% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_cps}
\alias{load_cps}
\alias{load_basic}
\alias{load_may}
\alias{load_org}
\alias{load_org_sample}
\title{Load a selection of EPI CPS extracts}
\usage{
load_cps(
  .sample,
  .years,
  ...,
  .extracts_dir = NULL,
  .version_check = TRUE,
  .quiet = getOption("epiextractr.quiet", FALSE)
)

load_basic(
  .years,
  ...,
  .extracts_dir = NULL,
  .version_check = TRUE,
  .quiet = getOption("epiextractr.quiet", FALSE)
)

load_may(
  .years,
  ...,
  .extracts_dir = NULL,
  .version_check = TRUE,
  .quiet = getOption("epiextractr.quiet", FALSE)
)

load_org(
  .years,
  ...,
  .extracts_dir = NULL,
  .version_check = TRUE,
  .quiet = getOption("epiextractr.quiet", FALSE)
)

load_org_sample(
  .years,
  ...,
  .extracts_dir = NULL,
  .version_check = TRUE,
  .quiet = getOption("epiextractr.quiet", FALSE)
)
}
\arguments{
\item{.sample}{CPS sample ("org", "basic", "march", "may")}

\item{.years}{years of CPS data (integers), or a \code{cps_files} object}

\item{...}{tidy selection of variables to keep}

\item{.extracts_dir}{directory where EPI extracts are}

\item{.version_check}{when TRUE, confirm data are same version}

\item{.quiet}{Logical. Suppress informational messages? Defaults to
\code{getOption("epiextractr.quiet", FALSE)}.}
}
\value{
A tibble of CPS microdata
}
\description{
Select years and variables from the EPI CPS microdata extracts. These data
must first be downloaded using \code{download_cps()} or from
\url{https://microdata.epi.org}.
}
\details{
All columns are selected if \code{...} is missing.

\code{.years} accepts either integer years or the result of \code{cps_files()}.
When a \code{cps_files} object is passed, the files are read directly and
\code{.extracts_dir} is ignored.

\code{.extracts_dir} is required, but if NULL it will look for the environment variables

\if{html}{\out{<div class="sourceCode">}}\preformatted{EPIEXTRACTS_CPSBASIC_DIR
EPIEXTRACTS_CPSORG_DIR
EPIEXTRACTS_CPSMAY_DIR
}\if{html}{\out{</div>}}

which could be set in your .Renviron, for example.
}
\section{Functions}{
\itemize{
\item \code{load_cps()}: base function group

\item \code{load_basic()}: Load CPS Basic Monthly files

\item \code{load_may()}: Load CPS May files

\item \code{load_org()}: Load CPS ORG files

\item \code{load_org_sample()}: Load a demonstration sample of CPS ORG files; only useful for examples

}}
\examples{
# Load all columns from the demonstration sample
load_org_sample(2023:2024)

# Load a selection of columns
load_org_sample(2023:2025, year, month, female, wage)

# Use cps_files() for targets workflows:
org_files = cps_files("org_sample", 2023:2025)
load_org_sample(org_files, year, month, wage)

\dontrun{
# Load real CPS ORG data (requires downloaded extracts):
load_org(2010:2019, year, month, orgwgt, female, wage)
# This is equivalent to
load_cps("org", 2010:2019, year, month, orgwgt, female, wage)
}
}
